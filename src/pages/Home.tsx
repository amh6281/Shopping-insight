import OptionForm from "../components/OptionForm";
import RequiredForm from "../components/RequiredForm";

const Home = () => {
  return (
    <div>
      <RequiredForm />
      <OptionForm />
    </div>
  );
};

export default Home;
